// Command winthistle is the local, guided UI for batch-opening Lightning
// channels. Only the abort paths exist so far — see HANDOFF.md for build order.
package main

import "fmt"

func main() {
	fmt.Println("winthistle: abort paths only; no funding flow yet (see HANDOFF.md)")
}
